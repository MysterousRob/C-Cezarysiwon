# Zadanie 2
Uzupełnij klasę `Computer`:
- dodaj właściwość `RAM` z get i set typu liczbowego
- dodaj właściwość `HDD` z get i set  typu liczbowego
- dodaj właściwość `HasBacklitKeyboard`z get i set  typu logicznego
- dodaj właściwość tylko do odczytu `INFO` typu `string`, która zwraca opis komputera z właściwościami `RAM`, `HDD` i `HasBacklitKeyboard`.

Utwórz metodę statyczną `ComputerDemo` w klasie `Program.cs`.
Zaprezentuje w niej działanie klasy `Computer`.