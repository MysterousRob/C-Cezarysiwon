# Zadanie 1
Zdefiniuj w klasie `Circle`:
- pola prywatne `_radius` typu liczbowego, `_x` i `_y` typu liczbowego
- konstruktor z jednym parametrme, który jest wartością promiemia.
  Przypisz  w konstruktorze parametr do pola `_radius`,
- metodę `Print`, która wyświetli informację o kole w postaci:
  "Koło o promieniu <_radius> ze środkiem w punkcie <_x, _y>"
W pliku `Program.cs` utwórz metodę statyczną `CircleDemo`. 
Utwórz w niej obiekt klasy `Circle` i wywołaj na obiekcie metodę `Print`.