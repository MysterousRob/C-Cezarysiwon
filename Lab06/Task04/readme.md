# Zadanie 4

Zdefiniuj metody statyczne, które zwracają obiekt klasy `Weight`:
- `ofKg(value)`z jednostką "kg"
- `ofGram(value)`z jednostką "g"
- `ofTon(value)` z jednostką "t"
- `ofDag(value)`  z jednostką "dak"
Zdefiniuj publiczną składową statyczną o nazwie `OneKg`, która jest obiektem z `_weight` równym 1 i jednostką "kg"
W klasie odwołuj się do jednostek za pomoca tablicy _units np. "kg" to _units[2]

Utwórz metodę statyczną `WeightDemo` w pliku Program.cs.
W metodzie zaprezentuj działanie metod statycznych.