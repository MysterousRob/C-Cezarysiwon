<Window x:Class="Laboratorium_12_Player.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Laboratorium_12_Player"
        mc:Ignorable="d"
        Padding="10"
        Title="MainWindow"
        MaxHeight="800"
        MinHeight="400"
        MinWidth="500">

    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="Background" Value="BlueViolet" />
            <Setter Property="Foreground" Value="Wheat" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="8" />
                </Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="Gray" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>

        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <MediaElement
            x:Name="MediaElement" 
            LoadedBehavior="Manual" 
            UnloadedBehavior="Stop" />
        <ListBox 
            Grid.Row="1"
            MinHeight="200"
            x:Name="SongList"
            SelectionChanged="SongList_OnSelected"
            VerticalAlignment="Top">
            <ListBoxItem>One</ListBoxItem>
            <ListBoxItem>Two</ListBoxItem>
        </ListBox>
        <TextBlock Grid.Row="2"
            x:Name="SongTitle">
            Hello
        </TextBlock>
        <Slider
            Grid.Row="3"
            Height="30"
            x:Name="ProgressSlider" Minimum="0" Maximum="1" Value="0" 
            ValueChanged="ProgressSlider_ValueChanged" />
        <Grid
            Grid.Row="4"
            Height="Auto"
            Width="Auto">
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Slider
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Value="0.5"
                Minimum="0"
                Maximum="1"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                x:Name="VolumeSlider" />
            <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource ButtonStyle}" IsEnabled="False"
                    x:Name="BtnPlay" Content="Play" Margin="5" Click="btnPlay_Click" />
            <Button Grid.Row="0" Grid.Column="3" Style="{StaticResource ButtonStyle}" x:Name="BtnPause" Content="Pause"
                    Margin="5" Click="btnPause_Click" />
            <Button Grid.Row="0" Grid.Column="4" Style="{StaticResource ButtonStyle}" x:Name="BtnStop" Content="Stop"
                    Margin="5" Click="btnStop_Click" />
            <Button Grid.Row="0" Grid.Column="5" Style="{StaticResource ButtonStyle}" x:Name="BtnOpen" Content="Open"
                    Margin="5" Click="BtnOpen_Click" />
        </Grid>
    </Grid>
</Window>